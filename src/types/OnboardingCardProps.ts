type OnboardingCardProps = {
  description: string;
  isDesktop: boolean;
  illustrationHeight: number;
  imageSource: string;
  index: number;
  screenWidth: number;
  title: string;
};

export type { OnboardingCardProps };
